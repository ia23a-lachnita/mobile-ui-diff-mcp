export const ANDROID_DEVICE_ID_PATTERN = "^[A-Za-z0-9._:-]+$";
export const IOS_SIMULATOR_ID_PATTERN = "^[A-Za-z0-9._:-]+$";

export const ANDROID_DEVICE_ID_REGEX = new RegExp(ANDROID_DEVICE_ID_PATTERN);
export const IOS_SIMULATOR_ID_REGEX = new RegExp(IOS_SIMULATOR_ID_PATTERN);
